name: _dash_skill_tool
description: Helper tools for managing and validating skills.
version: 1.0.0
environment:
  sdk: ^3.9.0
resolution: workspace

dependencies:
  yaml: ^3.1.0
  args: ^2.4.0
  path: ^1.9.0

dev_dependencies:
  logging: ^1.3.0
  skills_lint: ^0.5.1
  test: ^1.24.0
  test_process: ^2.1.1
